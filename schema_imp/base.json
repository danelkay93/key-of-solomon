{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Base Schema Definitions",
  "description": "Provides common reusable definitions for Arkham Horror LCG schemas.",
  "definitions": {
    "Code": {
      "description": "A unique, short identifier code (lowercase alphanumeric).",
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z0-9]+$"
    },
    "Name": {
      "description": "A human-readable name.",
      "type": "string",
      "minLength": 1
    },
    "Position": {
      "description": "An integer representing order or position, starting from 1.",
      "type": "integer",
      "minimum": 1
    },
    "ID": {
      "description": "A unique integer identifier.",
      "type": "integer",
      "minimum": 1
    },
    "DescriptionText": {
      "description": "A generic text field for descriptions.",
      "type": "string",
      "minLength": 1
    },
    "ImageURL": {
      "description": "A URL pointing to an image resource.",
      "type": "string",
      "format": "uri"
    }
  }
}
